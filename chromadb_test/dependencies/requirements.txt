Run the following commands on a Debian 12 system - 

curl -fsSL https://ollama.com/install.sh | sh

sudo apt install git

sudo apt install python3.11-venv

pip packages: 
curl 
rsync
ollama
chromadb
fpdf
python-docx
reportlab
